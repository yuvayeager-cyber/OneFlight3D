# Reproducible non-Torch runtime for the VGGT pipeline image and Colab.
# Torch/torchvision are installed separately from the CUDA 12.1 index because
# VGGT deliberately does not declare a CUDA-specific PyTorch dependency.
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
Pillow==10.4.0
opencv-python-headless==4.10.0.84
tqdm==4.66.5
pyproj==3.6.1
ultralytics==8.3.0
open3d==0.18.0
trimesh==4.4.9
laspy[lazrs]==2.5.4
rasterio==1.3.10
huggingface_hub==0.24.7
einops==0.8.0
safetensors==0.4.5
