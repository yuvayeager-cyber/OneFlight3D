# OneFlight3D — Core Dependencies
# Python 3.10+ required

# === Frame extraction & image I/O ===
opencv-python>=4.8
Pillow>=9.0

# === Data handling ===
numpy>=1.24
pandas>=2.0
scipy>=1.10
tqdm>=4.60

# === GPS / CRS transforms ===
pyproj>=3.5

# === YOLOv8 dynamic-object masking ===
ultralytics>=8.0

# === VGGT reconstruction (core engine) ===
# Install VGGT separately per their docs:
#   pip install vggt
# or from source:
#   git clone https://github.com/facebookresearch/vggt && cd vggt && pip install -e .
# Requires PyTorch with CUDA:
torch>=2.0

# === 3D reconstruction & mesh export ===
open3d>=0.17
trimesh>=3.20

# === Point cloud export (LAS) ===
laspy>=2.5

# === GeoTIFF DSM export ===
rasterio>=1.3

# === Mesh format support ===
# glTF/glb: handled by trimesh (built-in)
# FBX: requires one of:
#   pip install pyfbx          (lightweight, may have limitations)
#   OR use Blender headless:   blender --background --python export_fbx.py
# trimesh will attempt pyfbx first; if unavailable, the exporter
# falls back to Blender headless and warns.
